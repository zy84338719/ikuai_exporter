apiVersion: v1
kind: Secret
metadata:
  name: ikuai-credentials
  namespace: monitoring
type: Opaque
stringData:
  addr: "http://10.10.30.254"

  # ── v4 令牌模式（推荐）──────────────────
  token: "YOUR_API_TOKEN"

  # ── 用户名/密码模式（v3 或 v4）──────────
  # username: "admin"
  # password: "admin"
